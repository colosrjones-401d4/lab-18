'use strict';

/**
 * Converts data to a string and changes all characters to uppercase
 * @param {*} data - data to be modified
 */
function toUpperCase(data) {
  return data.toString().toUpperCase();
}

module.exports = toUpperCase;